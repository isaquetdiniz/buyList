describe("Authentication", () => {
  it("Should received JWT token when authenticated with valid credentials", async () => {
    const x = 2;
    const y = 6;
    const sum = x + y;
    expect(sum).toBe(8);
  });
});
